""" Car Wash Manager - pygame version ط¨ط§ط²ظ†ظˆغŒط³غŒ ظ†ط³ط®ظ‡â€ŒغŒ Tkinter ط¨ط§ pygame. ظ†ع©ط§طھ طھط؛غŒغŒط± ظ†ط³ط¨طھ ط¨ظ‡ ظ†ط³ط®ظ‡â€ŒغŒ Tkinter: - pygame ظˆغŒط¬طھ ط¢ظ…ط§ط¯ظ‡ (Entry/Button/Messagebox/Toplevel) ظ†ط¯ط§ط±ط¯طŒ ظ¾ط³ ظ‡ظ…ظ‡ ع†غŒط² (ط¯ع©ظ…ظ‡طŒ ط¨ط§ع©ط³ ظˆط±ظˆط¯غŒ ظ…طھظ†طŒ ظ¾ط§ظ¾â€Œط¢ظ¾â€Œظ‡ط§غŒ ط´ط¨غŒظ‡ messagebox/simpledialogطŒ ط§ط³ع©ط±ظˆظ„ طھط§ط±غŒط®ع†ظ‡) ط¯ط³طھغŒ ظ¾غŒط§ط¯ظ‡â€Œط³ط§ط²غŒ ط´ط¯ظ‡. - simpledialog ط¯ط± Tkinter "ظ…ظˆط¯ط§ظ„" ط§ط³طھ (ط¨ط±ظ†ط§ظ…ظ‡ ط±ط§ ط¨ظ„ط§ع© ظ…غŒâ€Œع©ظ†ط¯). ط§غŒظ†â€Œط¬ط§ ط¨ط§ غŒع© ط³غŒط³طھظ… Modal ط´ط¨غŒظ‡â€Œط³ط§ط²غŒ ط´ط¯ظ‡: ظˆظ‚طھغŒ غŒع© ط¯غŒط§ظ„ظˆع¯ ط¨ط§ط² ط§ط³طھ ط¨ظ‚غŒظ‡â€ŒغŒ طµظپط­ظ‡ ظ‚ط§ط¨ظ„ ع©ظ„غŒع© ظ†غŒط³طھ ظˆ ظپظ‚ط· ط¯غŒط§ظ„ظˆع¯ ع©غŒط¨ظˆط±ط¯/ظ…ط§ظˆط³ ط±ط§ ظ…غŒâ€Œع¯غŒط±ط¯. - ط§غŒظ…ظˆط¬غŒâ€Œظ‡ط§غŒ غŒظˆظ†غŒع©ط¯ (ًں“…ًںڑ— ...) ع†ظˆظ† ظپظˆظ†طھ ظ¾غŒط´â€Œظپط±ط¶ pygame ط¢ظ†â€Œظ‡ط§ ط±ط§ ط±ظ†ط¯ط± ظ†ظ…غŒâ€Œع©ظ†ط¯ ط­ط°ظپ ظˆ ط¨ط§ ط¨ط±ع†ط³ط¨ ظ…طھظ†غŒ ط³ط§ط¯ظ‡ ط¬ط§غŒع¯ط²غŒظ† ط´ط¯ظ†ط¯. - ط¨ط§ع©ط³ ظ…ظ‚ط¯ط§ط± (amount) ظپظ‚ط· ط±ظ‚ظ… ظ…غŒâ€Œظ¾ط°غŒط±ط¯ (ط¯ط± ظ†ط³ط®ظ‡â€ŒغŒ ط§طµظ„غŒ ظ‡ط± ع†غŒط²غŒ طھط§غŒظ¾ ظ…غŒâ€Œط´ط¯ ظˆ ظپظ‚ط· ظ…ظˆظ‚ط¹ ط«ط¨طھ ط§ط¹طھط¨ط§ط±ط³ظ†ط¬غŒ ظ…غŒâ€Œط´ط¯ط› ط§غŒظ†â€Œط¬ط§ ط¨ط±ط§غŒ طھط¬ط±ط¨ظ‡â€ŒغŒ ط¨ظ‡طھط± ط§ط² ظ‡ظ…ط§ظ† ط§ط¨طھط¯ط§ ظ…ط­ط¯ظˆط¯ ط¨ظ‡ ط±ظ‚ظ… ط´ط¯ظ‡ ط§ط³طھ). ط§ط¬ط±ط§: pip install pygame python carwash_manager_pygame.py """ import pygame import json import os import copy import sys PASSWORD = "4132" DATA_FILE = "carwash_manager.json" # ---------------- DEFAULT DATA ---------------- default_data = { "turn": 0, "workers": ["normand", "hajivand", "fardin", "reza", "ali", "mehdy", "meysam"], "today": {}, "history": [], "day_closed": False, "percent": {} } def create_worker_data(): return { "ghabz": 0, "amount": 0, "worker_share": 0, "carwash_share": 0, "tip": 0, "cars": [], "average": 0 } def create_new_data(): d = copy.deepcopy(default_data) for w in d["workers"]: d["today"][w] = create_worker_data() d["percent"][w] = 35 return d # ---------------- LOAD SAFE ---------------- if os.path.exists(DATA_FILE): try: with open(DATA_FILE, "r", encoding="utf-8") as f: data = json.load(f) if "workers" not in data: data = create_new_data() data.setdefault("today", {}) data.setdefault("percent", {}) data.setdefault("history", []) data.setdefault("turn", 0) data.setdefault("day_closed", False) for w in data["workers"]: if w not in data["today"]: data["today"][w] = create_worker_data() data["today"][w].setdefault("ghabz", 0) data["today"][w].setdefault("amount", 0) data["today"][w].setdefault("worker_share", 0) data["today"][w].setdefault("carwash_share", 0) data["today"][w].setdefault("tip", 0) data["today"][w].setdefault("cars", []) data["today"][w].setdefault("average", 0) data["percent"].setdefault(w, 35) except Exception: data = create_new_data() else: data = create_new_data() def save(): with open(DATA_FILE, "w", encoding="utf-8") as f: json.dump(data, f, ensure_ascii=False, indent=2) # ---------------- PYGAME INIT ---------------- pygame.init() pygame.key.set_repeat(400, 40) pygame.display.set_caption("Car Wash Manager") # ط§ط¨ط¹ط§ط¯ "ط¨ظˆظ… ظ…ط¬ط§ط²غŒ" ع©ظ‡ ع©ظ„ ط·ط±ط§ط­غŒ ط±ظˆغŒ ط¢ظ† ط§ظ†ط¬ط§ظ… ط´ط¯ظ‡ (ط·ط±ط§ط­غŒâ€Œط´ط¯ظ‡ ط¨ط±ط§غŒ ط­ط§ظ„طھ # ط¹ظ…ظˆط¯غŒ ع¯ظˆط´غŒ). ظ‡ظ…ظ‡â€ŒغŒ ط¯ع©ظ…ظ‡â€Œظ‡ط§/ظ…طھظ†â€Œظ‡ط§ ظ†ط³ط¨طھ ط¨ظ‡ ظ‡ظ…غŒظ† ط§ط¹ط¯ط§ط¯ ع†غŒط¯ظ‡ ط´ط¯ظ‡â€Œط§ظ†ط¯. WIDTH, HEIGHT = 420, 760 # طµظپط­ظ‡â€ŒغŒ ظˆط§ظ‚ط¹غŒ: ع©ظ„ ط±ط²ظˆظ„ظˆط´ظ† ط¯ط³طھع¯ط§ظ‡ ط±ط§ ظ…غŒâ€Œع¯غŒط±غŒظ… (ظپظˆظ„â€Œط§ط³ع©ط±غŒظ† ظˆط§ظ‚ط¹غŒطŒ ظ†ظ‡ غŒع© # ظ¾ظ†ط¬ط±ظ‡â€ŒغŒ ع©ظˆع†ع© ط¯ط± ع¯ظˆط´ظ‡â€ŒغŒ طµظپط­ظ‡). real_screen = pygame.display.set_mode((0, 0), pygame.FULLSCREEN) REAL_W, REAL_H = real_screen.get_size() # ظ‡ظ…ظ‡â€Œع†غŒط² ط±ظˆغŒ ط§غŒظ† ط¨ظˆظ… ط«ط§ط¨طھ ط±ط³ظ… ظ…غŒâ€Œط´ظˆط¯ط› ط¯ط± ظ¾ط§غŒط§ظ† ظ‡ط± ظپط±غŒظ… ط¨ط§ ط­ظپط¸ ظ†ط³ط¨طھ ط§ط¨ط¹ط§ط¯ # ط±ظˆغŒ ع©ظ„ طµظپط­ظ‡â€ŒغŒ ظˆط§ظ‚ط¹غŒ ع¯ظˆط´غŒ ع©ط´غŒط¯ظ‡ (scale) ظ…غŒâ€Œط´ظˆط¯ طھط§ ع©ظ„ طµظپط­ظ‡ ظ¾ط± ط´ظˆط¯. screen = pygame.Surface((WIDTH, HEIGHT)) clock = pygame.time.Clock() def virtual_to_real_rect(): """ظ…ط³طھط·غŒظ„غŒ ع©ظ‡ ط¨ظˆظ… ظ…ط¬ط§ط²غŒ (ط¨ط§ ط­ظپط¸ aspect ratio) ط¯ط§ط®ظ„ طµظپط­ظ‡â€ŒغŒ ظˆط§ظ‚ط¹غŒ ط¬ط§ ظ…غŒâ€Œط´ظˆط¯.""" scale = min(REAL_W / WIDTH, REAL_H / HEIGHT) sw, sh = max(1, int(WIDTH * scale)), max(1, int(HEIGHT * scale)) sx, sy = (REAL_W - sw) // 2, (REAL_H - sh) // 2 return pygame.Rect(sx, sy, sw, sh), scale def real_pos_to_virtual(pos): rect, scale = virtual_to_real_rect() x, y = pos return int((x - rect.x) / scale), int((y - rect.y) / scale) # ظپظˆظ†طھ ظ¾غŒط´â€Œظپط±ط¶ pygame (ظ…ط³طھظ‚ظ„ ط§ط² ط³غŒط³طھظ…â€Œط¹ط§ظ…ظ„طŒ ظ‡ظ…غŒط´ظ‡ ظ…ظˆط¬ظˆط¯ ط§ط³طھ) FONT = pygame.font.Font(None, 20) FONT_BOLD = pygame.font.Font(None, 20) FONT_BOLD.set_bold(True) TITLE_FONT = pygame.font.Font(None, 30) TITLE_FONT.set_bold(True) SMALL_FONT = pygame.font.Font(None, 16) BG = (242, 242, 242) WHITE = (255, 255, 255) BLACK = (20, 20, 20) GRAY = (170, 170, 170) def wrap_lines(text, font, max_width): """ظ…طھظ† ط±ط§ ط¨ط± ط§ط³ط§ط³ ط¹ط±ط¶ ظ…ط´ط®طµ ط¨ظ‡ ع†ظ†ط¯ ط®ط· ظ…غŒâ€Œط´ع©ظ†ط¯ (ظˆ \\n ط±ط§ ظ‡ظ… ط±ط¹ط§غŒطھ ظ…غŒâ€Œع©ظ†ط¯).""" lines = [] for paragraph in str(text).split("\n"): if paragraph == "": lines.append("") continue words = paragraph.split(" ") line = "" for word in words: test = (line + " " + word).strip() if font.size(test)[0] <= max_width: line = test else: if line: lines.append(line) line = word lines.append(line) return lines # ---------------- UI PRIMITIVES ---------------- class Button: def __init__(self, rect, text, bg, fg=(255, 255, 255), font=None): self.rect = pygame.Rect(rect) self.text = text self.bg = bg self.fg = fg self.font = font or FONT_BOLD self.visible = True def draw(self, surf): if not self.visible: return pygame.draw.rect(surf, self.bg, self.rect, border_radius=8) txt = self.font.render(self.text, True, self.fg) surf.blit(txt, txt.get_rect(center=self.rect.center)) def is_clicked(self, pos): return self.visible and self.rect.collidepoint(pos) class InputBox: def __init__(self, rect, digits_only=False, password=False): self.rect = pygame.Rect(rect) self.text = "" self.digits_only = digits_only self.password = password self.active = False def handle_keydown(self, event): if event.key == pygame.K_BACKSPACE: self.text = self.text[:-1] return None elif event.key in (pygame.K_RETURN, pygame.K_KP_ENTER): return "enter" elif event.key == pygame.K_ESCAPE: return "escape" return None def handle_textinput(self, event): ch = event.text if not ch: return if self.digits_only and not ch.isdigit(): return self.text += ch def draw(self, surf): pygame.draw.rect(surf, WHITE, self.rect, border_radius=4) border_color = (33, 150, 243) if self.active else GRAY pygame.draw.rect(surf, border_color, self.rect, 2, border_radius=4) shown = ("*" * len(self.text)) if self.password else self.text txt = FONT.render(shown, True, BLACK) surf.blit(txt, (self.rect.x + 10, self.rect.y + (self.rect.height - txt.get_height()) // 2)) class Modal: """ط¬ط§غŒع¯ط²غŒظ† messagebox / simpledialog طھع©غŒظ†طھط±. kind غŒع©غŒ ط§ط²: 'text' -> ظˆط±ظˆط¯غŒ ظ…طھظ†غŒ + ط¯ع©ظ…ظ‡ OK/Cancel 'int' -> ظˆط±ظˆط¯غŒ ظپظ‚ط· ط±ظ‚ظ… + ط¯ع©ظ…ظ‡ OK/Cancel 'confirm' -> ط³ظˆط§ظ„ ط¨ظ„ظ‡/ط®غŒط± 'message' -> ظپظ‚ط· ظ¾غŒط§ظ… + ط¯ع©ظ…ظ‡ OK """ def __init__(self, title, prompt, kind="text", on_ok=None, on_cancel=None, password=False, width=320): self.title = title self.kind = kind self.on_ok = on_ok self.on_cancel = on_cancel self.error = "" inner_w = width - 40 self.wrapped_prompt = wrap_lines(prompt, FONT, inner_w) top_pad = 50 content_h = len(self.wrapped_prompt) * 22 input_h = 46 if kind in ("text", "int") else 0 bottom_pad = 70 height = top_pad + content_h + input_h + bottom_pad height = max(180, min(height, HEIGHT - 60)) self.rect = pygame.Rect((WIDTH - width) // 2, (HEIGHT - height) // 2, width, height) if kind in ("text", "int"): self.input_box = InputBox( (self.rect.x + 20, self.rect.y + top_pad + content_h + 8, width - 40, 36), digits_only=(kind == "int"), password=password, ) self.input_box.active = True else: self.input_box = None btn_w, btn_h = 110, 38 by = self.rect.bottom - 50 if kind == "confirm": self.btn_ok = Button((self.rect.centerx - btn_w - 5, by, btn_w, btn_h), "Yes", (76, 175, 80)) self.btn_cancel = Button((self.rect.centerx + 5, by, btn_w, btn_h), "No", (244, 67, 54)) elif kind == "message": self.btn_ok = Button((self.rect.centerx - btn_w // 2, by, btn_w, btn_h), "OK", (33, 150, 243)) self.btn_cancel = None else: self.btn_ok = Button((self.rect.centerx - btn_w - 5, by, btn_w, btn_h), "OK", (76, 175, 80)) self.btn_cancel = Button((self.rect.centerx + 5, by, btn_w, btn_h), "Cancel", (244, 67, 54)) def submit(self): if self.kind == "text": value = self.input_box.text if self.on_ok: self.on_ok(value) return True elif self.kind == "int": raw = self.input_box.text.strip() if raw == "" or not raw.isdigit(): self.error = "Please enter a valid number" return False if self.on_ok: self.on_ok(int(raw)) return True else: if self.on_ok: self.on_ok(True) return True def cancel(self): if self.on_cancel: self.on_cancel() def handle_event(self, event): """True ط¨ط±ظ…غŒâ€Œع¯ط±ط¯ط§ظ†ط¯ ط§ع¯ط± ط§غŒظ† ظ…ظˆط¯ط§ظ„ ط¨ط§غŒط¯ ط¨ط³طھظ‡ ط´ظˆط¯.""" if self.input_box: if event.type == pygame.KEYDOWN: result = self.input_box.handle_keydown(event) if result == "enter": self.error = "" return self.submit() if result == "escape": self.cancel() return True elif event.type == pygame.TEXTINPUT: self.input_box.handle_textinput(event) if event.type == pygame.MOUSEBUTTONDOWN and event.button == 1: if self.btn_ok and self.btn_ok.is_clicked(event.pos): self.error = "" return self.submit() if self.btn_cancel and self.btn_cancel.is_clicked(event.pos): self.cancel() return True return False def draw(self, surf): overlay = pygame.Surface((WIDTH, HEIGHT), pygame.SRCALPHA) overlay.fill((0, 0, 0, 130)) surf.blit(overlay, (0, 0)) pygame.draw.rect(surf, WHITE, self.rect, border_radius=10) pygame.draw.rect(surf, GRAY, self.rect, 2, border_radius=10) title_surf = FONT_BOLD.render(self.title, True, BLACK) surf.blit(title_surf, (self.rect.x + 20, self.rect.y + 16)) y = self.rect.y + 50 for line in self.wrapped_prompt: surf.blit(FONT.render(line, True, BLACK), (self.rect.x + 20, y)) y += 22 if self.input_box: self.input_box.draw(surf) if self.error: err = SMALL_FONT.render(self.error, True, (200, 0, 0)) surf.blit(err, (self.rect.x + 20, self.rect.bottom - 78)) if self.btn_ok: self.btn_ok.draw(surf) if self.btn_cancel: self.btn_cancel.draw(surf) # ---------------- APP ---------------- class App: def __init__(self): self.running = True self.mode = "newday" if data.get("day_closed") else "main" self.modal = None self.history_worker = None self.history_scroll = 0 self.amount_box = InputBox((20, 95, WIDTH - 40, 40), digits_only=True) self.amount_box.active = True bw, bh, gap, start_y = WIDTH - 40, 46, 10, 150 self.btn_register = Button((20, start_y + (bh + gap) * 0, bw, bh), "Register", (76, 175, 80)) self.btn_next = Button((20, start_y + (bh + gap) * 1, bw, bh), "Next", (33, 150, 243)) self.btn_end = Button((20, start_y + (bh + gap) * 2, bw, bh), "End Day", (255, 152, 0)) self.btn_history = Button((20, start_y + (bh + gap) * 3, bw, bh), "History", (156, 39, 176)) self.btn_dashboard = Button((20, start_y + (bh + gap) * 4, bw, bh), "Dashboard", (0, 188, 212)) self.btn_admin = Button((20, start_y + (bh + gap) * 5, bw, bh), "Admin", (0, 0, 0)) self.btn_exit = Button((20, start_y + (bh + gap) * 6, bw, bh), "Exit", (244, 67, 54)) self.btn_newday = Button((20, 320, bw, 60), "Start New Day", (0, 150, 136)) self.btn_back = Button((20, 20, 90, 36), "Back", (110, 110, 110)) self.btn_clear_history = Button((20, 90, bw, 46), "Clear History", (244, 67, 54)) self.btn_edit_workers = Button((20, 150, bw, 46), "Edit Workers", (33, 150, 243)) self.btn_edit_percent = Button((20, 210, bw, 46), "Edit Percent", (76, 175, 80)) # ---- helpers to open modals ---- def ask_text(self, title, prompt, on_ok, on_cancel=None, password=False): self.modal = Modal(title, prompt, kind="text", on_ok=on_ok, on_cancel=on_cancel, password=password) def ask_int(self, title, prompt, on_ok, on_cancel=None): self.modal = Modal(title, prompt, kind="int", on_ok=on_ok, on_cancel=on_cancel) def ask_confirm(self, title, prompt, on_ok): self.modal = Modal(title, prompt, kind="confirm", on_ok=on_ok) def show_message(self, title, text): self.modal = Modal(title, text, kind="message", on_ok=lambda v: None) # ---- logic (same business rules as the Tkinter version) ---- def register(self): if not data["workers"]: self.show_message("Error", "No workers configured") return txt = self.amount_box.text.strip() if not txt.isdigit(): self.show_message("Error", "Amount must be numeric") return amount = int(txt) if amount <= 0: self.show_message("Error", "Amount must be greater than zero") return worker = data["workers"][data["turn"]] percent = data["percent"].get(worker, 35) def after_tip(car_type, tip_txt): tip_txt = tip_txt.strip() if tip_txt == "": tip = 0 elif tip_txt.isdigit(): tip = int(tip_txt) else: self.show_message("Error", "Tip must be numeric") return base_worker = int(amount * percent / 100) carwash_share = amount - base_worker worker_share = base_worker + tip info = data["today"][worker] info["ghabz"] += 1 info["amount"] += amount info["tip"] += tip info["worker_share"] += worker_share info["carwash_share"] += carwash_share info["cars"].append(car_type) if info["ghabz"] > 0: info["average"] = info["amount"] // info["ghabz"] data["turn"] = (data["turn"] + 1) % len(data["workers"]) self.amount_box.text = "" save() def after_car_type(car_type): if not car_type: return self.ask_text("Tip", "Enter tip amount (0 if none):", on_ok=lambda tip_txt: after_tip(car_type, tip_txt)) self.ask_text("Car Type", "What type of car?", on_ok=after_car_type) def next_turn(self): if data["workers"]: data["turn"] = (data["turn"] + 1) % len(data["workers"]) save() def start_new_day(self): for w in data["workers"]: data["today"][w] = create_worker_data() data["turn"] = 0 data["day_closed"] = False save() self.mode = "main" self.show_message("New Day", "New day started") def end_day(self): if data["day_closed"]: return def on_ok(date): if not date: return snapshot = {w: copy.deepcopy(data["today"][w]) for w in data["workers"]} data["history"].append({"date": date, "workers": snapshot}) data["history"] = data["history"][-30:] data["day_closed"] = True save() self.mode = "newday" self.show_message("Saved", "Day saved") self.ask_text("Date", "Enter date (1405/04/02):", on_ok=on_ok) def show_history_start(self): def on_ok(w): w = w.strip() if not w: return if w not in data["workers"]: self.show_message("Error", "Worker not found") return self.history_worker = w self.history_scroll = 0 self.mode = "history" self.ask_text("Worker", "Enter worker name:", on_ok=on_ok) def dashboard(self): total_amount = total_worker = total_carwash = total_tip = total_bills = 0 for w in data["workers"]: info = data["today"][w] total_amount += info["amount"] total_worker += info["worker_share"] total_carwash += info["carwash_share"] total_tip += info["tip"] total_bills += info["ghabz"] text = (f"Bills: {total_bills}\n" f"Income: {total_amount}\n" f"Tips: {total_tip}\n" f"Workers: {total_worker}\n" f"Carwash: {total_carwash}") self.show_message("Today Report", text) def admin_panel_start(self): self.ask_text("Admin", "Password:", password=True, on_ok=self.admin_check_password) def admin_check_password(self, pwd): if pwd != PASSWORD: self.show_message("Error", "Wrong password") return self.mode = "admin" def clear_history(self): def on_yes(_): data["history"] = [] save() self.show_message("Done", "History cleared") self.ask_confirm("Confirm", "Delete all history?", on_ok=on_yes) def edit_workers(self): def on_ok(txt): if not txt: return workers = [w.strip() for w in txt.split(",") if w.strip()] if not workers: return data["workers"] = workers for w in workers: if w not in data["today"]: data["today"][w] = create_worker_data() if w not in data["percent"]: data["percent"][w] = 35 for w in list(data["today"].keys()): if w not in workers: del data["today"][w] for w in list(data["percent"].keys()): if w not in workers: del data["percent"][w] data["turn"] = 0 save() self.show_message("Done", "Workers updated") self.ask_text("Workers", "Enter workers comma separated:", on_ok=on_ok) def edit_percent(self): def after_percent(w, p): if p < 0 or p > 100: self.show_message("Error", "Percent must be between 0 and 100") return data["percent"][w] = p save() self.show_message("Done", "Percent updated") def after_worker(w): w = w.strip() if not w: return if w not in data["workers"]: self.show_message("Error", "Worker not found") return self.ask_int("Percent", "Enter percent (0-100):", on_ok=lambda p: after_percent(w, p)) self.ask_text("Worker", "Worker name:", on_ok=after_worker) # ---------------- HISTORY RENDER ---------------- def build_history_blocks(worker): blocks = [] totals = dict(worker=0, carwash=0, bills=0, tip=0, amount=0) for day in reversed(data["history"]): info = day["workers"].get(worker, {}) cars = info.get("cars", []) cars_text = " , ".join(cars) if cars else "No Cars" lines = [ f"Date: {day['date']}", f"Cars: {cars_text}", f"Bills: {info.get('ghabz', 0)}", f"Amount: {info.get('amount', 0)}", f"Tip: {info.get('tip', 0)}", f"Worker: {info.get('worker_share', 0)}", f"Carwash: {info.get('carwash_share', 0)}", f"Average: {info.get('average', 0)}", ] blocks.append({"lines": lines, "bg": WHITE, "border": (60, 60, 60)}) totals["worker"] += info.get("worker_share", 0) totals["carwash"] += info.get("carwash_share", 0) totals["bills"] += info.get("ghabz", 0) totals["tip"] += info.get("tip", 0) totals["amount"] += info.get("amount", 0) total_lines = [ f"TOTAL - {worker}", f"Bills: {totals['bills']}", f"Amount: {totals['amount']}", f"Tips: {totals['tip']}", f"Worker: {totals['worker']}", f"Carwash: {totals['carwash']}", ] blocks.append({"lines": total_lines, "bg": (223, 240, 216), "border": (100, 150, 100)}) return blocks def draw_history(app): app.btn_back.draw(screen) title = FONT_BOLD.render(f"{app.history_worker} - 30 Day Report", True, BLACK) screen.blit(title, (120, 28)) top, pad = 70, 10 card_w = WIDTH - 2 * pad visible_h = HEIGHT - top blocks = build_history_blocks(app.history_worker) measured = [] total_height = 0 for block in blocks: wrapped = [] for line in block["lines"]: wrapped.extend(wrap_lines(line, SMALL_FONT, card_w - 20)) h = 16 + len(wrapped) * 18 measured.append((wrapped, h, block["bg"], block["border"])) total_height += h + 10 max_scroll = max(0, total_height - visible_h) app.history_scroll = max(0, min(app.history_scroll, max_scroll)) prev_clip = screen.get_clip() screen.set_clip(pygame.Rect(0, top, WIDTH, visible_h)) y = top - app.history_scroll for wrapped, h, bg, border in measured: if y + h >= top and y <= HEIGHT: rect = pygame.Rect(pad, y, card_w, h) pygame.draw.rect(screen, bg, rect, border_radius=6) pygame.draw.rect(screen, border, rect, 1, border_radius=6) ty = y + 8 for line in wrapped: f = FONT_BOLD if (line.startswith("TOTAL") or line.startswith("Date:")) else SMALL_FONT screen.blit(f.render(line, True, BLACK), (pad + 10, ty)) ty += 18 y += h + 10 screen.set_clip(prev_clip) if not data["history"]: empty = FONT.render("No history yet.", True, (120, 120, 120)) screen.blit(empty, (pad + 10, top + 10)) # ---------------- MAIN SCREENS ---------------- def draw_main(app): title = TITLE_FONT.render("Car Wash Manager", True, BLACK) screen.blit(title, title.get_rect(center=(WIDTH // 2, 35))) if data["workers"]: worker = data["workers"][data["turn"]] percent = data["percent"].get(worker, 35) turn_text = f"{worker} - {percent}%" else: turn_text = "No workers" turn_surf = FONT_BOLD.render(turn_text, True, BLACK) screen.blit(turn_surf, turn_surf.get_rect(center=(WIDTH // 2, 70))) app.amount_box.draw(screen) for b in (app.btn_register, app.btn_next, app.btn_end, app.btn_history, app.btn_dashboard, app.btn_admin, app.btn_exit): b.draw(screen) def draw_newday(app): title = TITLE_FONT.render("Car Wash Manager", True, BLACK) screen.blit(title, title.get_rect(center=(WIDTH // 2, 35))) info = FONT.render("Day is closed.", True, BLACK) screen.blit(info, info.get_rect(center=(WIDTH // 2, 220))) app.btn_newday.draw(screen) def draw_admin(app): app.btn_back.draw(screen) title = FONT_BOLD.render("Admin Panel", True, BLACK) screen.blit(title, (130, 28)) app.btn_clear_history.draw(screen) app.btn_edit_workers.draw(screen) app.btn_edit_percent.draw(screen) # ---------------- INPUT HANDLING ---------------- def handle_click(app, pos): if app.mode == "main": app.amount_box.active = app.amount_box.rect.collidepoint(pos) if app.btn_register.is_clicked(pos): app.register() elif app.btn_next.is_clicked(pos): app.next_turn() elif app.btn_end.is_clicked(pos): app.end_day() elif app.btn_history.is_clicked(pos): app.show_history_start() elif app.btn_dashboard.is_clicked(pos): app.dashboard() elif app.btn_admin.is_clicked(pos): app.admin_panel_start() elif app.btn_exit.is_clicked(pos): app.running = False elif app.mode == "newday": if app.btn_newday.is_clicked(pos): app.start_new_day() elif app.mode == "admin": if app.btn_back.is_clicked(pos): app.mode = "main" elif app.btn_clear_history.is_clicked(pos): app.clear_history() elif app.btn_edit_workers.is_clicked(pos): app.edit_workers() elif app.btn_edit_percent.is_clicked(pos): app.edit_percent() elif app.mode == "history": if app.btn_back.is_clicked(pos): app.mode = "main" # ---------------- MAIN LOOP ---------------- def main(): app = App() while app.running: for event in pygame.event.get(): if event.type == pygame.QUIT: app.running = False continue # ESC ط¨ط±ط§غŒ ط®ط±ظˆط¬ ط³ط±غŒط¹ (ظ…ظپغŒط¯ ط±ظˆغŒ ط¯ط³ع©طھط§ظ¾ ظ…ظˆظ‚ط¹ طھط³طھط› ط±ظˆغŒ ع¯ظˆط´غŒ ط¯ع©ظ…ظ‡â€ŒغŒ # Exit ط®ظˆط¯ظگ ط¨ط±ظ†ط§ظ…ظ‡ ط§ط³طھظپط§ط¯ظ‡ ظ…غŒâ€Œط´ظˆط¯). ظپظ‚ط· ظˆظ‚طھغŒ ظ…ظˆط¯ط§ظ„غŒ ط¨ط§ط² ظ†غŒط³طھ. if event.type == pygame.KEYDOWN and event.key == pygame.K_ESCAPE and not app.modal: app.running = False continue # ع†ظˆظ† طµظپط­ظ‡â€ŒغŒ ظˆط§ظ‚ط¹غŒ ظپظˆظ„â€Œط§ط³ع©ط±غŒظ† ط§ط³طھ ظˆظ„غŒ ط·ط±ط§ط­غŒ ط±ظˆغŒ ط¨ظˆظ…غŒ ط¨ط§ ط§ظ†ط¯ط§ط²ظ‡â€ŒغŒ # ط«ط§ط¨طھ WIDTH x HEIGHT ط§ظ†ط¬ط§ظ… ط´ط¯ظ‡طŒ ظ…ع©ط§ظ† ظ„ظ…ط³/ع©ظ„غŒع© ظ…ط§ظˆط³ ط±ط§ ط§ط² ظ…ط®طھطµط§طھ # ظˆط§ظ‚ط¹غŒ طµظپط­ظ‡ ط¨ظ‡ ظ…ط®طھطµط§طھ ظ‡ظ…ط§ظ† ط¨ظˆظ… ظ…ط¬ط§ط²غŒ طھط¨ط¯غŒظ„ ظ…غŒâ€Œع©ظ†غŒظ…. if event.type in (pygame.MOUSEBUTTONDOWN, pygame.MOUSEBUTTONUP): vpos = real_pos_to_virtual(event.pos) event = pygame.event.Event(event.type, button=event.button, pos=vpos) elif event.type == pygame.MOUSEMOTION: vpos = real_pos_to_virtual(event.pos) event = pygame.event.Event(event.type, pos=vpos, rel=event.rel, buttons=event.buttons) # ظˆظ‚طھغŒ ط¯غŒط§ظ„ظˆع¯غŒ ط¨ط§ط² ط§ط³طھطŒ ظپظ‚ط· ط®ظˆط¯ط´ ط±ظˆغŒط¯ط§ط¯ظ‡ط§ ط±ط§ ظ…غŒâ€Œع¯غŒط±ط¯ (ط±ظپطھط§ط± ظ…ظˆط¯ط§ظ„) if app.modal: modal_before = app.modal closed = modal_before.handle_event(event) if closed and app.modal is modal_before: app.modal = None continue if event.type == pygame.MOUSEBUTTONDOWN and event.button == 1: handle_click(app, event.pos) elif event.type == pygame.KEYDOWN and app.mode == "main" and app.amount_box.active: result = app.amount_box.handle_keydown(event) if result == "enter": app.register() elif event.type == pygame.TEXTINPUT and app.mode == "main" and app.amount_box.active: app.amount_box.handle_textinput(event) elif event.type == pygame.MOUSEWHEEL and app.mode == "history": app.history_scroll -= event.y * 30 screen.fill(BG) if app.mode == "main": draw_main(app) elif app.mode == "newday": draw_newday(app) elif app.mode == "admin": draw_admin(app) elif app.mode == "history": draw_history(app) if app.modal: app.modal.draw(screen) # ط¨ظˆظ… ظ…ط¬ط§ط²غŒ (420x760) ط±ط§ ط¨ط§ ط­ظپط¸ ظ†ط³ط¨طھ ط§ط¨ط¹ط§ط¯طŒ ط¨ط²ط±ع¯â€Œظ†ظ…ط§غŒغŒ ظˆ ظˆط³ط·â€Œع†غŒظ† # ط±ظˆغŒ ع©ظ„ طµظپط­ظ‡â€ŒغŒ ظˆط§ظ‚ط¹غŒ ع¯ظˆط´غŒ ط±ط³ظ… ظ…غŒâ€Œع©ظ†غŒظ… (ط¨ظ‚غŒظ‡â€ŒغŒ ظپط¶ط§ ظ…ط´ع©غŒ ظ…غŒâ€Œظ…ط§ظ†ط¯طŒ ط§ع¯ط± # ظ†ط³ط¨طھ ط§ط¨ط¹ط§ط¯ طµظپط­ظ‡â€ŒغŒ ع¯ظˆط´غŒ ط¯ظ‚غŒظ‚ط§ظ‹ ط¨ط±ط§ط¨ط± ط¨ظˆظ… ظ…ط¬ط§ط²غŒ ظ†ط¨ط§ط´ط¯). real_screen.fill((0, 0, 0)) target_rect, _ = virtual_to_real_rect() scaled = pygame.transform.smoothscale(screen, target_rect.size) real_screen.blit(scaled, target_rect.topleft) pygame.display.flip() clock.tick(30) pygame.quit() sys.exit() if __name__ == "__main__": main()
